--- a/net/minecraft/world/entity/ItemBasedSteering.java
+++ b/net/minecraft/world/entity/ItemBasedSteering.java
@@ -10,8 +_,8 @@
     private static final int MAX_BOOST_TIME = 700;
     private final SynchedEntityData entityData;
     private final EntityDataAccessor<Integer> boostTimeAccessor;
-    private boolean boosting;
-    private int boostTime;
+    public boolean boosting;
+    public int boostTime;
 
     public ItemBasedSteering(final SynchedEntityData entityData, final EntityDataAccessor<Integer> boostTimeAccessor) {
         this.entityData = entityData;
@@ -34,6 +_,14 @@
         return true;
     }
 
+    // CraftBukkit start
+    public void boost(final int boostTime) {
+        this.boosting = true;
+        this.boostTime = 0;
+        this.entityData.set(this.boostTimeAccessor, boostTime);
+    }
+    // CraftBukkit end
+
     public void tickBoost() {
         if (this.boosting && this.boostTime++ > this.boostTimeTotal()) {
             this.boosting = false;
@@ -44,7 +_,7 @@
         return this.boosting ? 1.0F + 1.15F * Mth.sin((float)this.boostTime / this.boostTimeTotal() * Mth.PI) : 1.0F;
     }
 
-    private int boostTimeTotal() {
+    public int boostTimeTotal() {
         return this.entityData.get(this.boostTimeAccessor);
     }
 }
